(** Student-written pathological bots (Part 3, section 1). Each bot lives in
    its own file under [app/bots/src/] and is re-exported here so other
    libraries can reach it as [Jsip_bots.<Bot_name>]. *)

module Cancel_storm = Cancel_storm
